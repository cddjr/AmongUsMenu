#include "pch-il2cpp.h"
#include "_hooks.h"
#include "logger.h"

bool dSaveManager_GetPurchase(String* itemKey, String* bundleKey, const MethodInfo* method) {
	/*if (bundleKey && bundleKey->fields.m_stringLength > 0) {
		STREAM_DEBUG("bundle:" << convert_from_string(bundleKey));
		return true;
	}
	if (itemKey && itemKey->fields.m_stringLength > 0) {
		STREAM_DEBUG("item:" << convert_from_string(itemKey));
		return true;
	}
	return false;*/
	return true;
}